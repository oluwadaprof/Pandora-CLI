appId: com.pandora.terminal
productName: Pandora Terminal
copyright: Copyright © 2024
directories:
  output: dist
  buildResources: build
files:
  - dist/**/*
  - package.json
  - electron/**/*
asar: true
protocol:
  name: Pandora Terminal
  schemes: ['pandora']
win:
  target:
    - target: nsis
      arch:
        - x64
    - target: portable
      arch:
        - x64
  requestedExecutionLevel: highestAvailable
  signAndEditExecutable: true
  certificateFile: ${env.WINDOWS_PFX_FILE}
  certificatePassword: ${env.WINDOWS_PFX_PASSWORD}
nsis:
  oneClick: false
  perMachine: true
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Pandora Terminal
  uninstallDisplayName: Pandora Terminal
  artifactName: PandoraTerminal-${version}.${ext}
mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  hardenedRuntime: true
  gatekeeperAssess: false
  notarize: true
  notarizeOptions:
    appBundleId: com.pandora.terminal
    appleId: ${env.APPLE_ID}
    appleIdPassword: ${env.APPLE_ID_PASSWORD}
    teamId: ${env.APPLE_TEAM_ID}
linux:
  target:
    - AppImage
    - deb
    - rpm
  category: Development
  desktop:
    Terminal: true
    Type: Application
    Categories: Development;System;TerminalEmulator
    MimeType: x-scheme-handler/pandora
  permissions:
    - allow-file-access
    - allow-network-access
    - allow-process-management
protocols:
  - name: Pandora Terminal
    schemes:
      - pandora
forceCodeSigning: true
publish:
  provider: github
  owner: PandoraTerminal
  repo: pandora-terminal
  private: false
  releaseType: draft